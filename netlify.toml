[build]
  publish = "."
  command = ""
  ignore = "git diff --quiet HEAD^ HEAD -- index.html vendora-login.html vendora-dashboard.html"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
